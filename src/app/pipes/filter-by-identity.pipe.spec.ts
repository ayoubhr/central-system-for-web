import { FilterByIdentityPipe } from './filter-by-identity.pipe';

describe('FilterByIdentityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByIdentityPipe();
    expect(pipe).toBeTruthy();
  });
});
